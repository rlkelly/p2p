pub fn copy_stub() {
//     let mut buf = BytesMut::new();
//     let (stream, addr) = listener.accept().await?;
//     let mut file = tokio::fs::File::open(
//         "/Users/user2/Documents/music/Jeromes Dream/Completed 1997-2001/12 How Staggering Is This Realization.mp3"
//     ).and_then(|mut file_in| {
//
//     });
//     file.read_buf(&mut buf).await.unwrap();
//     use std::io::SeekFrom;
//     // file.seek(SeekFrom::Start(1)).await;
//     tokio::io::copy(&mut file_buf, &mut writer).await;
}
